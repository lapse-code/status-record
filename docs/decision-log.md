# Decision Log

## 2026-06-11: 先做本地优先网页应用

决定：第一版实现为网页应用，本地保存数据。

原因：

- 用户希望先用起来验证流程。
- 网页开发速度快，便于调整记录口径。
- 后续可以包装成 Mac app，或评估 iOS/iPad 方案。

影响：

- MVP 不依赖云服务。
- 必须提供数据导出。
- 数据层需要为未来迁移保留边界。

## 2026-06-11: 拖延通过到岗时间自动计算

决定：不让用户手填拖延。用户到工位后点击到岗，系统记录 `arrived_at`，第一轮番茄钟开始时计算拖延。若用户未提前到岗而直接开始番茄钟，系统会把开始番茄钟的时刻同步记录为到岗时刻，并把拖延记为 0。内部字段仍沿用 `startup_delay`。

原因：

- 用户担心手填会美化自己。
- 时间戳比回忆更可靠。

影响：

- 需要 Arrival 模块。
- 如果用户忘记点击到岗但直接开始番茄钟，系统无法恢复此前真实拖延，只能从开始番茄钟这一刻同步到岗并记录 0 分钟拖延。

## 2026-06-11: MVP 记录注意力切换次数

决定：暂不记录“深度工作分钟”，改为每轮结束输入注意力切换次数。

原因：

- 深度工作分钟很难自我准确估算。
- 切换次数更容易回忆，也更适合长期观察趋势。

影响：

- 统计中使用注意力切换总次数和每小时切换次数。
- 后续如果接入自动检测，再扩展深度工作分钟。

## 2026-06-11: 状态、产物、不专注原因都使用标签系统

决定：五种结束状态、可见产物、不专注原因都通过 labels 管理。内部标签类型仍沿用早期的 `blocker` 命名。

原因：

- 用户希望数据化，同时保留扩展能力。
- 标签能支持统计图，也能适应后续习惯变化。

影响：

- 不把状态写死成不可变 enum。
- 需要标签管理页面或设置区。

## 2026-06-11: 每 25 分钟专注获得 5 分钟休息余额

状态：已被 2026-06-12 的“休息余额改为每日学习累计账本”替代。此条保留为历史决策记录。

决定：完成专注后按 `floor(actual_duration_minutes / 25) * 5` 增加休息余额。

原因：

- 用户不一定每 25 分钟立刻休息。
- 余额机制比强制休息更符合实际使用。

影响：

- 需要 Break Bank 模块。
- 复盘时询问立即使用还是保存。

## 2026-06-11: 提前完成按实际有效专注分钟记录

决定：手动提前点击完成时，`actual_duration_minutes` 按 `completed_at - started_at - paused_total_seconds - active_pause_seconds` 计算，向下取整到分钟，并且不超过计划时长。

原因：

- 避免刚开始就点击完成却记录完整计划时长。
- 休息余额必须跟真实有效专注分钟绑定。

影响：

- 自然倒计时结束仍记录计划时长。
- 暂停中的完成不会把暂停时间计入专注时长。

## 2026-06-11: 睡眠每天只记录一次

决定：睡眠记录只包含睡眠时长和主观精力 1-5，同一天重复输入时修改旧记录。

原因：

- 用户明确不想输入入睡和起床时间。
- 降低每日记录成本。

影响：

- `sleep_logs.local_date` 需要唯一约束。
- 统计以日为粒度。

## 2026-06-11: 初始化 Web MVP 技术栈

决定：使用 React + TypeScript + Vite 实现第一版网页应用，IndexedDB 通过 Dexie 管理，图表使用 Recharts。

原因：

- 当前项目不需要后端。
- 本地优先更适合个人私密记录。
- Vite 开发速度快，React 组件适合快速迭代复盘和统计界面。

影响：

- 项目依赖安装在本项目 `node_modules/`，不需要全局安装 Vite。
- 数据默认保存在浏览器本地，需要通过导出功能备份。
- 后续如加同步，应通过 service/repository 边界替换存储层。

## 2026-06-11: E2E 使用系统 Chrome

决定：Playwright 主流程测试使用本机 Google Chrome channel。

原因：

- 本机已安装 Google Chrome。
- 避免下载 Playwright 自带 Chromium。

影响：

- 本机运行 `npm run e2e` 需要有 Google Chrome。
- CI 环境如果没有系统 Chrome，需要改回 Playwright 托管浏览器并安装浏览器依赖。

## 2026-06-11: 睡眠录入改为 15 分钟步进控件

决定：睡眠录入界面不再使用小数小时，也不再拆成两个数字输入或下拉框；改为 `6:00`、`6:15`、`6:30` 这种 15 分钟粒度的步进输入，默认 `7:00`。底层仍保存 `sleep_duration_minutes`。精力评分也使用同样的步进输入，范围 1-5。

原因：

- `6.75 小时` 这种显示不符合日常理解。
- 用户更容易用固定步长微调，也可以直接键盘输入。

影响：

- 历史数据无需迁移。
- 历史值如果不是 15 分钟整，界面显示时会对齐到最近的 15 分钟。
- 统计口径不变。

## 2026-06-13: 睡眠和精力步进方向按直觉调整

决定：睡眠时长和主观精力步进控件统一改为上箭头增加、下箭头减少。键盘 `ArrowUp` 和 `ArrowDown` 与按钮方向保持一致。

原因：

- 用户反馈原来“上减、下加”不符合日常数字输入直觉。
- 睡眠时长和精力评分都属于数值控件，应采用同一套增减方向，减少误操作。

影响：

- 睡眠时长上箭头增加 15 分钟，下箭头减少 15 分钟。
- 精力上箭头增加 1 分，下箭头减少 1 分，仍限制在 1-5。
- E2E 中睡眠记录流程继续覆盖 15 分钟步进和精力步进。

## 2026-06-11: 增加示例数据操作

决定：增加“示例数据”操作，向当前浏览器 IndexedDB 写入 2026-06-01 到 2026-06-10 的 demo 记录。入口位置随布局变化，大屏可在侧栏，侧栏隐藏时在主内容区域显示。

原因：

- 用户需要不用手动录入就能查看统计页效果。
- 数据保存在浏览器本地，必须由应用自身写入当前浏览器上下文。

影响：

- 重复点击会先清理 `demo-` 前缀记录，再重新写入 demo 数据。
- 不删除用户真实记录。

## 2026-06-11: 记录明细跟随状态、产物、不专注原因图筛选

决定：统计页中“状态分布”“产物标签”“不专注原因”的饼图扇区和右侧标签列表都可以点击，并驱动下方“记录明细”的筛选。

原因：

- 底部区域不应只服务可见产物，也应能回看不专注原因和状态对应的复盘记录。
- 用户需要从任意分布图里的某个类别直接切到对应记录，而不是手动在长列表里找。

影响：

- 点击状态、产物或不专注原因标签后，下方记录只显示该标签关联的复盘记录。
- 切换到其他标签会立即切换筛选。
- 每条记录同时展示状态、产物、不专注原因、专注时长、注意力切换次数和可用文本备注。
- 2026-06-15 后，这个筛选交互只在统计页日视图保留；周/月视图当时不显示记录明细。2026-06-19 后，周/月记录明细以日期选择器形式恢复，但不恢复筛选交互。

## 2026-06-11: 增加休息倒计时和休息结束提醒

状态：此条中的“休息结束后重新打开 arrival session”已被 2026-06-15 的“到岗改为完整工作周期”替代。此处保留为历史决策记录。

决定：复盘中的休息选项改为“保留休息”和“使用休息”。选择使用休息后创建 `break_sessions` 记录并启动休息倒计时；休息自然结束或提前结束后，回到番茄钟选择，开放到岗记录保持不变。

原因：

- 用户需要真正的休息倒计时和休息结束提醒，而不只是余额数字。
- 休息时间不应该计入下一轮拖延。
- 提前结束休息时，未用完的休息分钟应回到余额，避免因为提前继续学习而损失休息额度。

影响：

- 新增 `break_sessions` 本地表，Dexie schema 升级到 version 2。
- 此处的 `break_bank_transactions` 真相来源口径已被 2026-06-12 的每日学习累计账本替代；现在新赚取的休息从当天 focus sessions 动态计算，transactions 只记录使用和退回。
- 休息结束后，用户可以在番茄钟区选择下一轮固定或自定义时长继续。

## 2026-06-11: 增加日点阵统计图

决定：统计页增加“日点阵”，一天固定 288 个点，每 5 分钟一个点。初始形态为纵向每列 12 个点代表 1 小时，横向 24 列代表一天，时间顺序为从上到下，再进入下一列；后续已改为根据容器宽度自适应每列 30 分钟或 1 小时。

原因：

- 用户需要用类似方框点阵的方式快速看到一天中拖延、休息、专注和不专注分布。
- 5 分钟粒度足够细，又不会像分钟级图表一样过密。

影响：

- 红色表示拖延。
- 天蓝色表示休息。
- 绿色表示专注。
- 黄色表示不专注。
- 2026-06-15 后，统计页只有日视图显示日点阵；周/月视图不再显示点阵。

## 2026-06-11: 日点阵支持历史日期导航

决定：日点阵增加前一天、后一天、今天和日期输入控件。2026-06-15 后，统计页日视图中的点阵日期与统计日范围绑定；用户手动选择日期会同步切换统计日。

原因：

- 点阵的价值在于日复盘，必须能连续查看历史日期，而不只是当前统计范围自动选出的某一天。
- 日期输入能快速跳到任意一天，前后按钮适合连续浏览。

影响：

- 点阵日期不再独立于统计页日范围；周/月统计页不显示日点阵。
- 没有记录的日期仍显示完整 288 个空白点。

## 2026-06-11: 增加版本化 JSON 备份导入

决定：导出 JSON 改为版本化备份格式 `status-record.backup` version 1，并增加“导入”操作。导入支持当前备份格式，也兼容早期直接导出的 `AppSnapshot` 结构。

原因：

- 网页部署后，每个用户的数据仍保存在自己的浏览器 IndexedDB 中，必须有可恢复的备份路径。
- 未来如果增加账号系统，同一份格式可以作为本地数据上传云端的同步包基础。
- 备份结构使用数据库表名，比 UI 内部 camelCase snapshot 更稳定。

影响：

- 导入采用合并写入：同 `id` 更新，不同 `id` 新增；不会先清空本地数据。
- 导入前会检查每张表是数组，并检查主键字段。
- `sleep_logs.local_date` 有唯一约束；如果同一天已有不同 `id` 的睡眠记录，导入时保留本地 id 并写入导入内容。
- Playwright 已覆盖导入备份后统计页出现导入记录。

## 2026-06-11: 使用 GitHub Pages 部署静态网页应用

决定：第一版线上部署使用 GitHub Pages + GitHub Actions。`main` 分支 push 后，workflow 会安装依赖、运行单元测试、lint、构建 Vite `dist`，再通过 Pages artifact 发布。

原因：

- 当前应用是纯前端本地优先应用，不需要后端服务即可给朋友使用。
- 仓库已经在 GitHub，GitHub Pages 能减少额外平台账号和部署链路。
- 构建前跑测试和 lint，可以避免明显坏版本自动上线。

影响：

- Vite 在 `GITHUB_PAGES=true` 时使用 `/status-record/` 作为 `base`，适配仓库 Pages 子路径。
- 线上地址为 `https://lapse-code.github.io/status-record/`。
- 线上用户数据仍保存在各自浏览器 IndexedDB；同步仍依赖导出/导入，尚无账号系统。

## 2026-06-11: 未到岗直接开始番茄钟时自动到岗

决定：`startFocusTimer` 不再允许产生没有到岗记录的专注。用户可以先点“到岗”再开始，也可以直接点番茄钟开始；如果没有打开的 arrival session，系统会同步创建 arrival session，并把本轮 focus session 绑定到它。

原因：

- 用户的工作逻辑是“到岗后才能工作”，不能出现没有到岗上下文的专注记录。
- 直接点击番茄钟开始应被理解为“现在已经到岗并马上开始”，此时拖延为 0。

影响：

- 新增 E2E 覆盖“未到岗直接开始番茄钟”的路径。
- 在新的时间线口径中，自动到岗和开始时间相同，因此本轮开始前拖延为 0；后续如果到岗仍开放且没有专注或休息，等待时间会继续进入红色拖延。内部字段仍沿用 `startup_delay`。

## 2026-06-12: 主界面采用 Focus Studio 布局

决定：主界面从早期默认仪表盘样式改为 Focus Studio 方向：左侧导航和本地数据操作，主工作区优先展示专注倒计时、今日摘要、睡眠、日点阵和最近记录。后续 2026-06-15 已移除 Today 最近记录，复盘记录集中到统计页日视图。

原因：

- 用户希望日常使用不累，界面更像安静的个人学习工作台。
- 早期布局存在块不够整齐、空白不自然、点阵轴文字压得太紧等问题。
- Today 页面应服务真实操作，不应像营销页或松散卡片集合。

影响：

- Today 页面上方固定为专注倒计时和今日侧栏，下方用完整宽度展示日点阵；早期最近记录入口已在后续迭代移除。
- 分类查看和记录明细集中放在统计页日视图。
- 视觉风格保持克制的工作台色彩和 8px 以内圆角。

## 2026-06-12: 日点阵根据容器宽度自适应分列

决定：日点阵的源数据仍保持一天 288 个 5 分钟点，但 UI 根据容器宽度切换展示密度：宽容器每列 30 分钟，窄容器每列 1 小时。

原因：

- 统计页宽屏下使用固定 24 列会留下大量空白。
- 窄容器下压缩 48 列会让小时数字不可读。
- 自适应分列能在 Today 和 Analytics 两个不同容器里保持可读性。

影响：

- 点阵状态颜色和统计口径保持同一套定义：红色为拖延，天蓝色为休息，绿色为专注，黄色为不专注。
- 日期导航仍支持前一天、后一天、今天和直接输入日期。
- E2E 对点阵说明文案兼容“每列 30 分钟”和“每列 1 小时”两种结果。

## 2026-06-12: 倒计时结束触发浏览器通知和默认提示音

决定：番茄钟自然结束和休息倒计时自然结束时，系统同时触发浏览器通知、内置短提示音和页面内提示。

原因：

- 用户需要在页面不一定处于注意力中心时也能收到提醒。
- 番茄钟结束需要回到复盘，休息结束需要回到下一轮拖延和专注选择。
- 不引入自定义音频文件，先使用浏览器通知和 Web Audio 生成的短提示音。

影响：

- `src/reminders.ts` 封装提醒逻辑，主界面只在计时自然结束时调用。
- 用户手动点击“完成”或“提前结束休息”不会触发系统通知，避免误提醒。
- 如果浏览器通知权限不可用或被拒绝，页面内提示和复盘弹窗仍然生效。

## 2026-06-12: 日点阵格子颜色改为多数时长决定

决定：日点阵仍然显示 5 分钟一个格子，但每个格子内部先按细粒度时长累计状态，再用多数时长决定最终颜色。只有在状态时长打平时，才使用当前点阵状态优先级决定颜色。当前实现使用秒级状态累计，避免某个状态只碰到一分钟的一小段就被放大成整分钟。

原因：

- 旧逻辑是“只要有重叠就按优先级覆盖”，会让 4 分钟拖延 + 1 分钟专注的格子显示成专注，不符合直觉。
- 多数时长规则更接近用户对这个 5 分钟区间主要状态的理解。

影响：

- 1 分钟红色拖延 + 4 分钟绿色专注会显示绿色。
- 4 分钟红色拖延 + 1 分钟绿色专注会显示红色。
- 2 分钟绿色专注 + 2 分钟黄色不专注 + 1 分钟空白会显示黄色。

## 2026-06-12: 休息自然结束后可继续使用剩余余额

决定：休息倒计时自然结束后，如果仍有可用休息余额，不再直接进入下一轮拖延，而是弹出“休息结束”选择。用户可以开始专注、继续休息 5 分钟、使用全部剩余休息，或输入自定义休息分钟。

原因：

- 用户可能先使用 5 分钟休息，但手上仍有可用余额。
- 休息结束后是否继续休息应由用户当下判断，而不是系统自动进入学习流程。
- “使用全部”按钮可以快速使用当前最长可用休息时间。

影响：

- `startBreakTimer` 支持复盘之外继续启动休息倒计时。
- 继续休息的最大分钟数是当前休息余额。
- 没有剩余休息余额时，休息结束回到番茄钟选择；如果用户仍到岗，等待时间继续显示为拖延。

## 2026-06-12: 新增周点阵页面

决定：在主导航中新增“周点阵”页面，按周一到周日展示 7 天日点阵，并提供上一周、下一周、本周和日期跳转。

原因：

- 单日点阵适合当天复盘，但用户需要横向比较一周内每天的时间分布。
- 现有 `buildDayTimeline` 已经稳定，周视图可以复用同一套颜色和多数时长规则。

影响：

- 不新增数据库表，也不影响导入导出格式。
- 周视图只负责展示和日期导航，源数据仍来自现有到岗、专注、复盘和标签记录。

## 2026-06-12: 日点阵加入休息状态并扩大拖延覆盖

决定：日点阵新增 `break` 状态，用天蓝色展示 `break_sessions` 记录的休息时间；arrival 区间内未被休息或专注覆盖的等待时间显示为红色拖延。内部状态名仍为 `startup_delay`。

原因：

- 休息和拖延都不是专注，但含义完全不同，不能共用红色。
- 用户到岗后或休息结束后还没开始下一轮专注时，应被看作拖延。
- 休息中不应被误算成拖延，这与 `break_sessions` 的设计一致。

影响：

- 日点阵和周点阵图例新增“休息”计数。
- 多数时长打平时的颜色优先级更新为 `blocked > focus > startup_delay > break > empty`。
- 不新增数据库表，也不改变导入导出格式；休息状态直接来自现有 `break_sessions`。

## 2026-06-12: 用户可见术语改为拖延、专注、不专注

决定：点阵和统计中的用户可见术语改为红色“拖延”、绿色“专注”、黄色“不专注”、天蓝色“休息”。原先的“阻塞原因”在界面中改为“不专注原因”。

原因：

- “阻塞”偏工程术语，普通用户不容易立刻理解。
- “专注/不专注/拖延”更接近用户每天复盘时的自然语言。
- 内部字段名和数据结构保持不变，可以继续兼容导入导出、历史数据和代码逻辑。

影响：

- `startup_delay` 内部字段在 UI 中显示为“拖延”。
- `focus` 点阵状态在 UI 中显示为“专注”。
- `blocked` 点阵状态和 `blocker` 标签类型在 UI 中显示为“不专注”或“不专注原因”。

## 2026-06-12: 数据操作入口不能依赖侧栏可见性

决定：示例数据、导入、导出是全局数据操作。大屏布局中可放在侧栏；当侧栏操作区被响应式布局隐藏时，必须在主内容顶部显示同等入口。

原因：

- 用户指出手机只是例子，任何隐藏侧栏的状态都不能让导入导出入口消失。
- 导入和导出关系到数据备份和恢复，不能依附在可能被隐藏的局部导航区域。

影响：

- `DataActions` 在侧栏和主内容备用区域复用。
- 隐藏的文件导入 input 与可见按钮解耦，避免某个布局下按钮可见但导入不可用。
- E2E 新增窄屏场景，验证侧栏入口隐藏时主内容入口仍可见，并且导入功能可用。

## 2026-06-12: 暂停进入拖延并记录专注片段

决定：暂停不是简单冻结倒计时。点击暂停时，系统关闭当前真实专注片段，并让后续时间继续落在打开的到岗记录中，因此点阵显示为拖延；点击继续时，在同一轮番茄钟下开启新的专注片段；点击完成时，按当前有效专注分钟结束本轮并进入复盘；点击取消时，本轮不进入专注统计。

原因：

- 用户期望暂停和完成都会先记录已经学过的时长。
- 暂停和完成的区别是：暂停之后仍可继续同一轮，完成会中断本轮。
- 旧模型只用 `started_at + actual_duration_minutes` 无法准确表示暂停前后多个真实专注区间，会把暂停时间误画成专注。

影响：

- 新增 `focus_segments` 表，当时 Dexie schema 升级到 version 3；后续时区迁移已升级到 version 4。
- 开始番茄钟时创建 running segment；暂停关闭 segment；继续创建新 segment；完成关闭当前 segment。
- 日点阵和周点阵优先使用 completed focus segments；旧数据没有 segments 时回退到旧连续区间。
- JSON 导出包含 `focus_segments`；导入旧备份缺少该表时按空数组兼容。

## 2026-06-12: 不专注原因图表排除“无”并优化窄屏布局

决定：“不专注原因”图表不再把默认“无”显示为一个原因。复盘记录和导入导出仍保留“无”，但图表展示层排除它；如果排除后没有任何原因，显示“暂无不专注原因记录”。统计页图表从固定三列改为自适应列宽，分布图卡片过窄时饼图和标签列表上下排列。

原因：

- “无”表示本轮没有不专注原因，不是一个需要分析的原因类别。
- 半屏或较窄窗口下，固定三列会让饼图和标签互相挤压，影响可读性。

影响：

- `blockerLabelCounts` 底层聚合仍保留“无”，避免破坏历史数据和备份格式。
- 只有 UI 中的“不专注原因”分布图排除“无”。
- 统计页分布图使用自适应布局，标签允许换行，饼图半径随容器缩放。

## 2026-06-12: 开放到岗记录必须保持单例

决定：到岗是一个开放状态。已有未关闭到岗记录时，重复点击到岗或开始番茄钟必须复用已有记录，不得新增 arrival session，也不得重置 `arrived_at`。如果历史数据里已经存在多条未关闭到岗记录，系统以最早打开的那条作为当前有效到岗；点击离开时，会把重复开放记录一并关闭。

原因：

- 用户认为到岗时间只能开启一次，关闭前重复开启应无效。
- 如果重复开放记录按最新一条显示，会让 UI 看起来像开始番茄钟重置了到岗时间，拖延统计也会被低估。
- 旧数据或旧版本可能已经留下重复开放记录，需要在读取和关闭时兜底。

影响：

- `startFocusTimer` 绑定最早未关闭 arrival session。
- Today 页面展示最早未关闭 arrival session。
- `checkOutArrival` 会关闭全部未关闭 arrival session，恢复“最多一个开放到岗”的状态；休息切换不关闭到岗记录。
- E2E 覆盖已有到岗开始番茄钟不重置，以及重复开放到岗时绑定最早记录。

## 2026-06-15: 到岗改为完整工作周期

决定：arrival session 表示用户从“到岗”到“离开”的完整工作周期，不再表示单轮番茄钟之间的拖延切段。复盘保存、选择使用休息、继续休息、休息自然结束、提前结束休息和开始下一轮番茄钟都不得关闭、重开或重置开放到岗记录。用户点击“离开”会写入 `left_at`；未到岗直接开始番茄钟时仍可自动创建一条新的到岗记录。2026-06-20 起，启用拖延保护时，连续拖延达到上限也会自动写入 `left_at`。

原因：

- 用户希望到岗时间代表完整工作周期，包含拖延、专注、不专注、休息和摸鱼等待，而不是每轮之间被系统切开。
- 自动关闭再重开到岗会让 UI 看起来像到岗时间被刷新，削弱拖延记录的可信度。
- 现有点阵已经用“到岗底色 + 专注/休息/不专注覆盖”的算法，可以在一个长到岗周期内准确区分各种状态。

影响：

- `submitSessionReview` 不再在保存复盘或使用休息时关闭或重开 arrival session。
- `startBreakTimer` 和 `completeBreakTimer` 不再写 `arrival_sessions`。
- 休息区间仍由 `break_sessions` 覆盖为天蓝色，休息之外的开放到岗等待继续显示为红色拖延。
- E2E 新增断言：复盘保存、进入休息、提前结束休息后，开放到岗记录的 `id` 和 `arrived_at` 保持不变。

## 2026-06-12: 休息余额改为每日学习累计账本

决定：休息奖励不再按单轮 `floor(actual_duration_minutes / 25) * 5` 独立计算，而是按“每日学习累计账本”计算。当天所有已完成番茄钟的真实有效分钟累计到同一个账本，每满 25 分钟获得 5 分钟今日休息余额；未满 25 分钟的进度和未使用休息余额都不跨天保留。`break_bank_transactions` 不再写入新的 `earned` 交易，只记录使用休息和提前结束退回；已赚休息从当天 `focus_sessions.actual_duration_minutes` 动态计算。

原因：

- 用户希望 10 分钟 + 15 分钟也能跨轮累计成 25 分钟并获得 5 分钟休息。
- 用户希望取消当前轮时，本轮不会进入学习累计，也不会产生休息余额。
- 休息余额不应该变成长期账户，第二天应重新开始。

影响：

- `completeFocusTimer` 的 `earned_break_minutes` 表示本轮让今日累计新跨过几个 25 分钟门槛。
- Today 页面显示今日休息余额和 `x / 25` 的今日进度。
- 旧备份中的 `earned` 交易仍保留导入导出；如果没有 focus sessions 可动态计算，会按旧 `earned` 交易兼容。
- 单元测试新增每日账本、跨轮累计和跨天清零覆盖；E2E 新增 15 + 15 分钟跨轮获得休息覆盖。

## 2026-06-12: 点阵实时显示当前进行中的状态

决定：日点阵和周点阵不只读取已复盘的历史结果，也会把当前正在发生的状态纳入计算。打开的到岗记录会延伸到当前 `now` 并显示为拖延；运行中的专注片段会延伸到当前 `now` 和本轮计划专注时长预算两者中更早的时间，并显示为专注；运行中的休息倒计时显示为休息；暂停后只保留暂停前已关闭的专注片段，暂停区间回到拖延；完成但尚未复盘的专注先显示为专注，保存复盘后再按最终标签改为专注或不专注；取消的专注片段不进入点阵。

原因：

- 用户需要点阵反映“此刻系统认为正在发生什么”，而不是等整轮结束和复盘保存后才变化。
- 当前 5 分钟格子的颜色应继续遵守多数时长规则，例如前 2 分钟拖延、后 3 分钟专注时显示专注。
- 不专注只能在复盘后判断，不能在专注过程中提前预测。
- 取消本轮意味着这段临时专注颜色必须撤销。

影响：

- `buildDayTimeline` 接受 `now`，并纳入 running focus segment、running break session 和开放 arrival session。
- Today 页、统计页日点阵和周点阵都会传入 App 的每秒 `now` 状态。
- 单元测试新增实时拖延、实时专注、暂停拖延、取消撤销、完成未复盘和实时休息覆盖。

## 2026-06-14: 点阵改为真实时长守恒并与拖延统计同源

决定：`buildDayTimeline` 不再把一天展开成整天的分钟或秒状态数组，而是直接按 5 分钟 cell 计算源记录与 cell 的真实毫秒级重叠时长。每个 cell 输出 `durationMsByState` 和 `parts`：`durationMsByState` 用于统计和图例，`parts` 用于在 UI 中渲染混合分段。`state` 仍保留为 cell 主状态，由多数时长决定，但不能再作为统计口径。“今日拖延”和统计页拖延趋势必须从日点阵 `startup_delay` 真实时长汇总得到，等于点阵红色真实时长总和。

原因：

- 用户导出的 2026-06-14 数据里，一次 5 分钟休息从 15:37:21 开始，跨过了 15:40 的 cell 边界。固定 5 分钟单色格如果只看主状态，可能看起来像两个蓝点；如果强行限制成一个点，又会丢掉跨边界的真实休息时间。
- 正确原则是时间守恒：5 分钟休息仍然只统计 5 分钟，但可以在两个相邻 cell 中以分段形式展示它各自占用的部分。
- 用户同时发现 Today 卡片的“今日拖延”和点阵红色视觉不一致。根因是旧统计只算“到岗到第一轮开始”，而点阵红色包含完成后、暂停中、休息后等待下一轮等全部在岗未专注时间。
- 直接按 cell 计算重叠区间，比分钟级触碰更接近真实时间占比，也避免实时刷新时每次创建 86400 个秒状态导致页面变卡。

影响：

- 点阵图例显示真实分钟，不再显示主色点数量。
- 跨 5 分钟边界的休息会以混合分段显示；真实休息总时长仍由 `actual_duration_minutes` 或计划休息分钟封顶，不因浏览器回调延迟膨胀。
- `totalStartupDelayMinutes` 和 trend 的 `startupDelayMinutes` 改为从 `sumTimelineCellDurations(buildDayTimeline(...)).startup_delay` 得到。
- 取消的 focus session 和 canceled focus segment 不进入点阵；如果到岗仍打开，取消后的时间自然回到红色拖延，同时每日休息账本因为只读取 completed/reviewed focus sessions，会自动回滚本轮贡献。
- 新增单元测试覆盖拖延统计与点阵红色时长同源、跨边界休息真实时长守恒、取消专注不贡献休息余额。

## 2026-06-12: 时间线按记录发生时区绘制

决定：按天归属的源记录保存 UTC 时间戳、`local_date` 和 IANA `time_zone`。新记录使用当前设备时区；旧数据或旧备份缺少 `time_zone` 时按 `Asia/Tokyo` 回填。日点阵和周点阵按每条记录自己的 `time_zone` 把 UTC 区间映射到目标日期的本地分钟，不用用户当前设备时区重新解释历史记录。

原因：

- 用户可能旅行或切换设备时区，如果历史 UTC 时间直接按当前时区展示，会把原本正常作息误显示成熬夜。
- `local_date` 只能解决日期归属，不能解决一天内部 00:00-23:59 的分钟位置；点阵需要知道记录发生时区。
- 记录发生时区是最稳定的默认口径，后续仍可增加“按当前位置时区查看”的显示模式。

影响：

- Dexie schema 升级到 version 4，为到岗、专注、分段、休息交易、休息倒计时和睡眠记录增加 `time_zone` 索引并迁移旧数据。
- 导出备份包含 `time_zone`；导入旧备份时自动回填 `Asia/Tokyo`。
- `buildDayTimeline` 按记录时区映射分钟；Today、统计页日点阵和周点阵标题会显示单一时区或“多时区”提示。
- 新增时间函数和单元测试，覆盖 UTC 时间在东京/洛杉矶的日期差异，以及按记录时区放置点阵格子。

## 2026-06-13: 标签页升级为设置页

决定：侧边栏入口从“标签”改为“设置”。设置页顶部管理点阵状态颜色；下方按状态、产物、不专注原因分组管理标签。每条标签默认只显示颜色、名称、正常/已归档状态、历史记录数量和齿轮设置按钮。齿轮弹窗负责改名、改颜色、保存、归档/解除归档和删除。

原因：

- 用户认为原来的“保存/隐藏”行内操作难以理解，且“隐藏”不能准确表达历史保留但新记录不可选的语义。
- 点阵图状态颜色属于系统显示设置，不应该混在新增标签表单里。
- 有历史数据但长期不用的标签需要归档；没有历史价值的标签才适合删除。

影响：

- `labels.is_active = false` 明确表示归档；归档标签不会出现在复盘弹窗中，但历史统计仍然保留。
- `deleteLabel` 会检查 `session_reviews.status_label_id` 和 `session_review_labels.label_id` 引用；已有历史记录的标签不能删除，只能归档。
- 点阵颜色保存到 `app_settings.timelineColors`，并随 JSON 导入导出一起备份。
- E2E 更新为覆盖新增并归档自定义标签。

## 2026-06-15: 专注判定绑定到保留状态

决定：点阵中已复盘专注片段的绿色/黄色只由复盘状态决定，不再由“不专注原因”标签参与判定。固定标签 id `status-completed` 是唯一的专注状态：选择它的复盘显示为绿色专注；其他状态显示为黄色不专注。`status-completed` 可以改名和改颜色，但不能归档或删除；设置弹窗需要明确提示它和专注状态绑定。

原因：

- 不专注原因是诊断标签，会随用户习惯不断新增、改名和归档，不适合作为点阵颜色的核心判定条件。
- 复盘状态比原因标签更稳定；用唯一保留状态作为绿色条件，长期统计更一致。
- 用户仍然可以在“完成”的复盘里选择不专注原因，用于记录过程中出现的小问题，但不改变本轮整体为专注的颜色。

影响：

- `buildDayTimeline` 对 reviewed focus session 只检查 `status_label_id === status-completed`。
- `updateLabel` 禁止把 `status-completed` 归档；`deleteLabel` 禁止删除 `status-completed`。
- 设置页的该标签不显示归档按钮，删除按钮禁用，并显示绑定说明。
- 单元测试覆盖“完成 + 非无不专注原因仍为绿色”和“自定义同名完成状态仍为黄色”；E2E 覆盖设置页保护状态。

## 2026-06-15: 统计页按日/周/月拆分展示层级

决定：统计页保留日、周、月三种范围，但展示层级分开。日视图显示当天统计、日点阵和当天记录明细，并允许点击状态、产物、不专注原因图表筛选明细。周/月视图当时只显示该范围聚合统计和图表，不显示日点阵和记录明细；2026-06-19 后，周/月记录明细以日期选择器形式恢复，但仍不显示日点阵。时间占比采用方案 A：专注 + 不专注 + 拖延 = 100%，休息不进入分母。

原因：

- 日点阵和记录明细是日复盘工具，放在周/月视图里会让用户误以为它们代表整个范围。
- 周/月更适合看趋势和比例，不适合塞入某一天的明细。
- 休息是恢复时间，不应稀释“专注、不专注、拖延”三者的行为占比。
- 统计页的专注、不专注、拖延必须和点阵真实状态时长同源，避免卡片、图例和点阵各算一套。

影响：

- `AnalyticsSummary` 增加 `totalBlockedMinutes`，trend 增加 `blockedMinutes`。
- `totalFocusMinutes`、`totalBlockedMinutes`、`totalStartupDelayMinutes` 都从 `buildDayTimeline` 的 `durationMsByState` 汇总得到。
- 统计页新增“时间占比”卡片，使用用户设置的点阵颜色。
- 周/月范围增加上一周/下一周/本周、上一月/下一月/本月控件；后续日/周/月范围控制统一移动到统计顶部右侧。
- E2E 覆盖月/周不显示日点阵、日视图筛选记录明细、导入后切到日视图查看导入记录；2026-06-19 后补充覆盖周/月按日期查看记录明细。

## 2026-06-15: 统计页统一日期控制并支持编辑记录明细

决定：统计页的日/周/月切换和日期范围控制统一放到顶部“统计”面板右侧。日视图的日点阵不再自带日期控制，只显示点阵和图例。日视图显示当天睡眠参数，不显示单日睡眠折线图；周/月视图才显示睡眠与精力趋势图。Today 页面移除重复的最近记录，复盘记录查看集中在统计页日视图。日视图记录明细新增编辑入口，可以修改已有复盘参数。

原因：

- 日期控制分散在统计顶部和日点阵内部时，日/周/月交互不一致。
- 单日睡眠折线图信息量低，用参数卡片更清楚；周/月才适合看趋势。
- Today 的最近记录和统计页记录明细重复，容易造成维护和阅读负担。
- 用户可能在番茄钟结束后点快了，需要事后修正复盘数据，但不应该影响真实计时和休息账本。

影响：

- `AnalyticsRangeControls` 支持日、周、月三种粒度：前一天/后一天/今天，上一周/下一周/本周，上一月/下一月/本月。
- `DayTimelinePanel` 支持隐藏日期控制；统计页日视图使用顶部控制，Today 页面仍保留点阵内日期控制。
- 新增 `updateSessionReview` 服务，只更新状态、注意力切换次数、产物标签/文字和不专注原因标签/文字；不修改专注时长、休息余额、番茄钟内部累计时钟或到岗记录。
- 复盘字段抽成共享 UI，结束复盘仍显示休息选择，记录明细编辑弹窗不显示任何休息相关字段。
- E2E 更新为使用统计顶部日期输入，并覆盖导入记录后的复盘明细编辑。

## 2026-06-15: 周/月统计改用轻量区间汇总

决定：日点阵继续由 `buildDayTimeline` 输出 288 个 5 分钟 cell；统计页周/月趋势和总计不再逐日生成完整点阵，而是复用同一套时间覆盖优先级，在整日区间上直接累计各状态真实毫秒数。统计计算先建立 `focusSegmentsByFocusId`、`reviewByFocusId` 和 `sleepByDate` 索引，避免每个日期、每个 focus session 反复扫描全部分段。

原因：

- 月统计只需要范围内的专注、不专注、拖延、睡眠和标签汇总，不需要每一天的 288 个视觉 cell。
- 旧实现中 `buildTrend` 对月范围内每一天调用 `buildDayTimeline`，会把“画点阵”的成本带进“看月统计”的路径；数据量变大后，点击月统计会在主线程做大量同步运算。
- 直接维护持久化周/月汇总表虽然读取最快，但编辑复盘、取消番茄钟、导入备份、跨时区显示和未来迁移都会增加一致性风险。当前阶段更适合保留原始记录为唯一真相，使用内存索引和轻量派生计算。

影响：

- `buildAnalyticsSummary` 会为本次计算创建内存索引，并通过轻量区间汇总生成 trend 中的专注、不专注和拖延分钟。
- 日视图统计仍能和日点阵 `durationMsByState` 对齐；新增单元测试验证混合拖延、休息、不专注场景下轻量汇总和日点阵真实时长一致。
- Analytics 周/月的 `now` 依赖按分钟更新；App 顶层在非 Today 页面不再每秒重算 Today summary 和 Today 点阵。
- 原始数据结构、导入导出格式和用户历史数据不变；这是计算路径优化，不是 schema 迁移。

## 2026-06-15: 统计图表视觉口径调整

决定：统计摘要卡片不再依赖位置选择颜色，改为每张卡片显式传入语义色。专注为绿色，不专注为黄色，拖延为红色，切换次数和每小时切换为黄色，平均精力保持默认文字色。月视图的“专注、不专注与拖延”趋势图横跨统计页整行，并使用堆叠柱状图；日视图睡眠模块改为全宽紧凑参数条，只展示睡眠时长和精力。

原因：

- 位置选择器在统计卡片顺序变化后容易把颜色语义搞错。
- 月范围包含 30/31 天，三组并列柱在普通三列卡片里会变成细线，缺乏可读性。
- 日视图睡眠只有两个参数，不应该占用一个大图表卡片，容易产生大量空白。

影响：

- `StatCard` 增加 `tone` 参数，避免未来因为排序或响应式换行导致颜色错位。
- 月视图趋势图在宽屏全行展示，窄屏仍自然单列；日/周仍保留普通对比柱状图。
- 日视图睡眠信息更紧凑，周/月仍使用睡眠与精力趋势图。

## 2026-06-15: Today 页面新增手动记录

决定：Today 页面新增“手动记录”入口，用于补录忘记开启番茄钟但已经完成的学习时间。手动记录不新增独立业务表，而是直接创建 `reviewed` focus session、completed focus segment、session review 和标签关系。界面复用复盘字段，但只增加“开始时间”和“持续时间”输入，不显示使用休息、保留休息或休息倒计时选项。

原因：

- 用户忘记开启番茄钟时，仍需要把真实学习时间补进当天统计。
- 手动记录本质上仍是一段已完成的学习时间，复用现有 focus/review/schema 可以自然进入导出、导入、日点阵、周/月统计和标签分布。
- 不使用独立表可以避免统计层同时合并两套学习记录来源，降低后续 Mac/iOS 迁移复杂度。
- 休息余额已经从当天 `focus_sessions.actual_duration_minutes` 动态计算，因此手动记录只要写入真实分钟，就能自动进入每日休息累计。

影响：

- 新增 `createManualFocusRecord` 服务；保存时不会创建、关闭、重开或修改 arrival session。
- 手动记录持续时间默认 25 分钟，支持直接输入，也支持上箭头增加 1 分钟、下箭头减少 1 分钟。
- 手动记录保存后直接刷新 Today 摘要、日点阵、统计、记录明细和休息余额。
- 手动记录时间段不能和已有专注记录重叠，避免点阵显示和休息余额重复计算。
- 新增 E2E 覆盖手动记录、休息余额增加和重叠时间段拒绝。

## 2026-06-15: 手动记录重叠校验对齐有效专注区间

决定：手动记录的重叠校验不再把 `focus_sessions.started_at` 到 `completed_at` 的整段墙钟时间都当成已有专注。校验优先读取 `focus_segments`，并用 `actual_duration_minutes` 或计划时长封顶，得到和点阵一致的有效专注区间；只有输入时间段和这些有效区间重叠时才拒绝保存。

原因：

- 浏览器后台、系统休眠或回调延迟可能让 `completed_at` 晚于真实有效专注结束时间。
- 点阵已经按有效专注预算截断这类异常拉长的 segment，用户看到 18:10 是拖延时，手动记录也应该允许覆盖这段拖延。
- 如果重叠校验继续使用整段墙钟时间，会出现“视觉上是拖延，但保存时提示和专注重叠”的口径冲突。

影响：

- 新增 `getEffectiveFocusOverlapRanges` 纯函数，供手动记录保存前校验使用。
- 手动记录仍然不能和真正的有效专注分钟重叠，避免休息余额和统计重复计算。
- 新增单元测试覆盖 `completed_at` 被异常拉长但 `actual_duration_minutes` 只有 25 分钟时，25 分钟之后允许补录。

## 2026-06-19: UI 刷新频率从业务计时中拆分

决定：App 不再用一个每秒更新的全局 `now` 同时驱动倒计时、点阵、周点阵和统计图。倒计时和自然结束检测保留 1 秒级刷新；Today 日点阵和周点阵改为约 30 秒低频刷新；统计页改为约 60 秒低频刷新。每次数据写入并重新加载 snapshot 后，会立即推进低频显示时间，保证用户操作后界面马上同步。

原因：

- 倒计时需要秒级体验，但点阵、周点阵和统计图不需要每秒重算。
- 旧路径会让 App 顶层每秒刷新时带着 Recharts 图表和 288 格点阵一起重建，数据量增大或浏览器半屏布局时容易出现主线程卡顿。
- 低频显示时间足以表达当前拖延、专注和休息的大致实时状态；真实时长仍由源记录的 UTC 时间戳和有效专注区间计算，不受刷新频率影响。

影响：

- `src/App.tsx` 拆出 `timerNow`、`timelineNow` 和 `analyticsNow`。
- 点阵、周点阵、统计、睡眠和设置页通过 `memo`、稳定回调和布局 `useMemo` 避免被倒计时秒级 tick 触发重组件重渲染。
- 数据口径不变：专注、拖延、休息余额、手动记录、点阵真实时长和周/月统计仍从同一套源数据派生。
- UI 上当前格子的颜色最长可能延迟到下一次低频 tick 更新，但任何开始、暂停、完成、复盘、手动记录、导入或刷新数据都会立即更新。

## 2026-06-19: 周/月记录明细以日期选择器恢复

决定：统计页周/月视图恢复底部“记录明细”，但不显示整段范围的所有记录，也不生成日点阵。周视图显示当前周的日期按钮；月视图显示当前月的日历式日期网格。用户点击某一天后，下方只展示该日期的复盘记录，并保留编辑入口。

原因：

- 用户需要从周/月统计中继续回看具体记录，但一次性铺开整个周期会难以阅读。
- 周/月记录明细应该服务“找某一天记录”，而不是把聚合图表变成日志列表。
- 当前性能优化后，周/月不能回到生成大量日点阵或重型实时计算的实现。

影响：

- 日视图继续支持点击状态、产物、不专注原因图表筛选记录。
- 周/月视图的分布图只作为聚合图表；记录明细通过日期选择器切换具体日期。
- 记录明细仍使用同一个编辑弹窗，只修改复盘参数，不修改专注时长、休息余额或计时记录。
- 实现只基于 `summary.reviewEntries` 按 `local_date` 分组和过滤，不调用 `buildDayTimeline`，不会重新引入周/月点阵计算成本。

## 2026-06-20: 连续拖延达到上限时自动退岗

决定：新增拖延保护规则，默认开启且默认上限为 15 分钟。开放到岗记录进入连续拖延后，App 设置一个被动 `setTimeout`；如果网页后台、合盖或系统睡眠导致定时器延迟，页面 focus/visibility 恢复时会补算。触发自动退岗时，`arrival_sessions.left_at` 写为“连续拖延开始时间 + 上限分钟”，`updated_at` 写为实际补写数据库的时间。

原因：

- 用户有时会忘记点击离开，开放到岗会让整段离开电脑后的时间继续被统计为拖延。
- 网页无法在 Mac 合盖或浏览器休眠时可靠执行代码，因此规则必须保证“数据结果等价”：恢复后补写时也不能把恢复时间当成业务离岗时间。
- 过去曾出现过 `completed_at` 被当成有效专注结束时间，导致点阵显示可补录但手动记录校验拒绝保存。自动退岗必须明确禁止把 `updated_at` 当成业务时间边界，避免同类字段混用问题。

影响：

- 新增 `app_settings.idleAutoCheckout`，结构为 `{ enabled, maxDelayMinutes }`；设置页提供启用开关和分钟数输入。
- 新增 `getIdleAutoCheckoutDecision` 纯函数，只基于源记录和传入的 `now` 判断当前是否连续拖延、何时应该退岗，不写数据库。
- 新增 `autoCheckoutIdleArrival` 服务函数，自动退岗时分开写 `left_at` 和 `updated_at`；普通手动离开仍使用同一时间写两个字段。
- 如果历史数据中存在多条未关闭到岗记录，自动退岗会和手动离开一样清理这些重复开放记录，并保证每条记录的 `left_at` 不早于自身 `arrived_at`。
- 自动退岗不新增拖延表，不改变点阵和统计核心算法；点阵和统计继续只读取业务时间字段，`updated_at` 只用于记录更新/导入/未来同步语义。
- 新增单元测试覆盖普通连续拖延、未达上限、专注/休息覆盖、专注后等待以及长时间后台恢复后的截断时间。

## 2026-06-20: 新增日历回看页面

决定：在侧边栏新增“日历”页面，导航顺序为今天、统计、周点阵、日历、设置。日历页面提供周/月两种互斥视图：周视图是 00:00-24:00 的七天时间线，只展示已经复盘的具体事项；月视图是月历格，只展示每天出现过的产物标签。

原因：

- 周点阵负责查看时间状态分布，用户还需要一个更像 Google Calendar / Apple Calendar 的视图来回看“具体做了什么”。
- 周视图和月视图的信息密度不同，不能同时铺在同一屏；周视图适合看具体事项的时间段和文字，月视图适合快速看某天有哪些产物类别。
- 日历不能重新引入拖延、休息或空白时间，否则会和点阵页重复。

影响：

- 新增 `src/domain/calendar.ts` 作为日历读模型，不新增数据库表。
- 周视图事件从已复盘 focus session 的有效专注区间生成，显示时间范围、产物标签和文字描述。
- 月视图按日期聚合产物标签，并对同一天重复出现的标签去重。
- 日历事件长度与点阵和手动记录重叠校验保持同一口径：优先读取 `focus_segments`，并受 `actual_duration_minutes` 或计划时长封顶约束；不能直接用 `completed_at - started_at`。

## 2026-06-20: 调整日历可读性和固定侧栏

决定：周日历从压缩时间轴改为更适合阅读的日程时间轴，一小时高度提高到能容纳两个半小时级事项；事件卡片优先显示时间、产物标签标题和复盘文字。大屏侧栏固定在视口内，底部数据操作区域不再随右侧长页面滚动；窄屏仍切换为顶部导航和主内容区数据操作入口。

原因：

- 用户的常见学习单位是 25 分钟番茄钟，原先一小时 58px 的时间轴无法同时容纳两段半小时事项，也无法显示复盘文字。
- 日历页的价值是回看具体做了什么，不能只显示产物标签。
- 右侧周日历很长时，侧栏如果随页面滚动会让导入/导出入口难以访问。

影响：

- 周日历纵向高度增加，查看全天需要滚动，但每个事项的可读性更高。
- 周点阵和日历使用不同图标，减少侧栏识别混淆。
- 侧栏固定只作用于大屏布局；响应式隐藏侧栏操作区时，主内容顶部仍提供同等数据操作入口。
